# PULSEFALL — 설계 문서

> 비트 위에서만 벽이 열린다.

---

## 0. 왜 갈아엎었나

기존 Pulsefall이 재미없던 이유는 취향 문제가 아니라 구조 문제였습니다. 코드를 열어서 확인한 것들:

| 증상 | 실제 원인 | 위치 |
|---|---|---|
| **렉** | `ctx.shadowBlur`을 게이트 드로우 루프 안에서 매 게이트마다 설정. 캔버스에서 shadowBlur가 걸린 draw는 **서피스 전체 블러 패스**를 강제합니다. 화면에 게이트 15개면 프레임당 전체화면 블러 15번. | `game.js:1684, 1746, 1774` |
| **렉** | `createRadialGradient`를 draw 함수 안에서 매 프레임 재생성 | `game.js:1895` |
| **억울한 죽음** | 고정 틱이 없음. 프레임 delta를 그대로 물리에 사용 → 프레임이 흔들리면 게임 속도가 흔들림 | `game.js:frame()` |
| **뻔함 / 판독 불가** | 장애물이 "원형 링 + 금색 아크 갭". 매번 다르게 생겼는데 매번 똑같이 플레이됨 → **학습 불가능** | 전반 |
| **죽은 화면** | 카메라 회전 제거. 자료조사 §8이 정확히 경고한 실수 — *"회전은 난이도를 올리는 동시에 미리 볼 시간을 준다"* | 의도적 제거 |
| **루즈함** | 60초 × 6행성 = 같은 짓 6분. 그리고 **플레이어가 하는 행동이 회피밖에 없음** | 구조 |
| **음악 낭비** | Analyser로 저역 에너지를 읽어 *반응*만 함. 벽이 비트 위에 있지 않음 | `game.js:updateMusicEnergy` |

성능은 고치면 되지만, 마지막 세 개는 고칠 수 있는 게 아니라 **다른 게임을 만들어야 하는** 문제였습니다.

---

## 1. 한 줄 정의

**PULSEFALL은 리듬 반사 아케이드입니다.** 벽 가장자리를 스칠수록 뜨거워지고, 막힌 링은 박자 위에서만 뚫립니다.

회전 회피 아케이드의 순수함 + 고정 레벨·진행률의 리텐션 구조 + 탄막 슈팅의 그레이즈 경제.

---

## 2. 핵심 설계 결정 4가지

### 2.1 동사를 하나 더 준다 — PULSE

순수 회피 게임의 유일한 약점은 **플레이어가 아무것도 하지 않는다**는 것입니다. 오직 피할 뿐입니다. 그래서 모든 플레이어의 최적 플레이가 똑같이 생깁니다.

PULSEFALL은 두 번째 동사를 넣습니다.

- **PULSE** = 0.26초 무적 + 각속도 2.75배. 벽을 통과합니다.
- 비용은 게이지 1칸. 게이지는 **그레이즈로** 찹니다.
- **박자에 맞춰 누르면 0.32칸.** 그리고 — 이게 핵심 규칙인데 — **박자 위에서는 게이지가 없어도 무조건 발동합니다.**

마지막 규칙(`ONBEAT_ALWAYS_ALLOWED`)이 게임 전체를 지탱합니다. 이유는 §3.3에.

### 2.2 안전 플레이는 0점 — GRAZE

- 벽 **가장자리** 0.2 라디안 안쪽을 지나가면 그레이즈. 게이지 + HEAT가 오릅니다.
- **점수 = 시간 × HEAT** (HEAT 1.0 ~ 8.0).
- 그레이즈를 1.15초 안 하면 HEAT가 깎이기 시작합니다.

즉 **넓은 틈 한가운데로 안전하게 지나가면 아무것도 못 얻습니다.** 살아남는 것과 잘하는 것이 분리되고, 공포와 탐욕이 매 링마다 충돌합니다. 이게 "뻔함"의 해독제입니다.

### 2.3 무한 생존을 버리고 트랙제로

이 장르의 고전들은 대체로 무한 생존입니다. 반면 **고정 레벨 + 진행률 %** 구조를 택한 리듬 액션 게임들이 훨씬 크게 성공했습니다. 이유는 명확합니다:

- 차트가 고정 → **68%에서 죽은 자리를 외울 수 있다**
- % 표시 → 실패해도 증분 성취가 보인다
- 즉시 재시도 → 실패 비용이 0

PULSEFALL의 차트는 `(트랙, 시드) → 벽 목록`으로 **결정론적 컴파일**됩니다. 같은 트랙은 영원히 같은 차트입니다.

### 2.4 음악을 재생하지 않고 합성한다

BGM은 mp3가 아니라 Web Audio 오실레이터로 **실시간 합성**됩니다.

- 라이선스 리스크 0 (앱스토어 제출 시 최대 블로커였음)
- 에셋 용량 0 바이트
- mp3 디코드로 인한 마이크로 스터터 없음 *(모바일에서 압축 음원을 런타임에 디코드할 때 생기는 알려진 문제)*
- **그리고 진짜 이유**: 음악과 차트가 **같은 객체**에서 나옵니다

```js
{ name: "DROP", bars: 8,
  drums: { kick: "x...x...x...x.x.", snare: "....x.....x.X...", hat: "xxx.xxx.xxx.xx.x" },
  bass: "0.0.3.0.5.0.3.0.",
  sides: 6, speed: 1.5, sub: 1, spin: 1, fx: { impact: true } }
```

드럼이 터지는 그 섹션 객체가 `speed`를 1.5로 올리고 `sub`를 1로 반으로 줄입니다. 난이도 스파이크가 음악에 *맞춰진* 게 아니라 **그냥 음악입니다.**

---

## 3. 게임 규칙 상세

### 3.1 좌표계

로직은 전부 **필드 공간**(중심 원점, 카메라 회전 무관)에서, 렌더만 **스크린 공간**으로 변환합니다. 자료조사 §5.1 그대로입니다. 화면이 아무리 돌아도 충돌 판정은 건드리지 않습니다.

모든 지오메트리는 **정규화 필드 단위**입니다 (1.0 = 기준 플레이 반지름).
기존 빌드는 픽셀로 튜닝해서 화면 크기마다 난이도가 조용히 달랐습니다.

| 상수 | 값 | 의미 |
|---|---:|---|
| `CORE_R` | 0.100 | 중앙 폴리곤 |
| `PLAYER_R` | 0.128 | 커서 궤도 |
| `SPAWN_R` | 1.620 | 벽 생성 |
| `PLAYER_SPEED` | 7.45 rad/s | 반 바퀴 ≈ 0.42초 *(자료조사 §9.3 기준치 0.35~0.5초)* |
| `PLAYER_HALF_W` | 0.070 rad | 커서 반각 — **그려지는 삼각형과 정확히 같음** |
| `HIT_FORGIVE` | 0.007 | 벽 양면에서 깎아내는 여유 |

### 3.2 벽은 항상 "변" 위에 있다

이게 기존 빌드와 가장 크게 갈리는 지점입니다. 벽은 임의의 호가 아니라 **다각형의 변(side)** 단위로만 존재합니다. 그래야 패턴에 **어휘**가 생깁니다 — spiral, pincer, staircase, cage. 어휘가 생겨야 학습이 되고, 학습이 돼야 실력이 늘고, 실력이 늘어야 다시 켭니다.

변의 개수는 섹션마다 3~9로 바뀝니다. **홀수 변에서는 좌우 거리가 비대칭**이라 회전 방향 선택이 생사를 가릅니다 *(자료조사 §2.5 경고)*.

### 3.3 벽은 비트에 "도착"한다 (생성이 아니라)

```
w.spawnTime = w.time − (SPAWN_R − PLAYER_R) / speed
```

`w.time`은 벽이 커서 궤도에 **닿는** 순간이고, 그게 비트입니다. 플레이어는 킥이 떨어지는 바로 그 순간에 틈을 통과합니다. 차트를 **보기 전에 들을 수 있습니다.**

그리고 이것이 §2.1의 규칙을 필연으로 만듭니다:

> 막힌 링은 비트에 도착한다 → 무적이어야 하는 순간이 곧 비트다 →
> **박자에 맞춘 PULSE를 거부하지 않으면, 막힌 링은 언제나 넘을 수 있다.**

게이지는 *생존의 관문*이 아니라 *탐욕의 보상*이 됩니다. 박자를 벗어난 PULSE는 여전히 1칸 전액을 받습니다 — 그게 배급제 패닉 버튼입니다.

### 3.4 카메라

회전은 복원했습니다. 자료조사가 못박은 대로 **회전은 난이도이자 동시에 정보**입니다. 회전 방향 반전 주기는 어떤 섹션에서도 2.4~7초 안으로 강제됩니다 — 그보다 잦으면 반대편 미리보기가 사라져서 오히려 불공정해집니다.

---

## 4. 공정성 솔버 (가장 중요한 안전장치)

차트 컴파일 후 `enforceFairness()`가 모든 링을 순회하면서 실제 도달 가능성을 검증하고, 불가능하면 **그리드 위에서 링을 통째로 회전**시킵니다. 패턴 모양(spiral은 spiral로)은 보존됩니다.

여기서 세 개의 실제 버그를 잡았습니다:

1. **접촉 창(contact window)** — 플레이어는 벽의 안쪽 면이 도착한 시점부터 바깥쪽 면이 지나갈 때까지 그 틈에 갇혀 있습니다. 도착~도착으로 예산을 잡으면 `thick/speed`초를 공짜로 주는 셈입니다. 두께 0.105 / 속도 1.4에서 75ms = **육각형 한 변의 절반 이상**. 불공정한 차트는 정확히 이런 오차로 만들어집니다.
2. **커서는 점이 아니라 쐐기** — 틈의 양쪽을 반각만큼 깎아야 합니다.
3. **도착 순서 ≠ 생성 순서** — 속도가 섞인 패턴(swing)은 나중 링이 앞 링을 추월합니다. 솔버는 도착 순서로 걸어야 합니다.

솔버 결과 (`npm run audit`):

```
FIRST LIGHT  161 walls · 106.6s · 152 rings ·  4 rotated · worst slack 0.11 rad
HOLLOW SUN   176 walls ·  83.4s · 173 rings · 37 rotated · worst slack 0.04 rad
VOID BLOOM   193 walls ·  71.9s · 190 rings · 70 rotated · worst slack 0.16 rad
```

### 4.1 헤드리스 오토플레이 봇

`scripts/bot.mjs`는 렌더 없이 240Hz로 차트를 실제로 플레이합니다. **선견지명은 링 하나뿐**인 일부러 멍청한 봇입니다. 이 봇이 클리어하지 못하는 차트는 숙련된 사람도 억울해할 차트입니다.

`npm test`가 3개 트랙 + 데일리 + 엔들리스 6시드를 전부 100% 클리어하는지 검증합니다. 자료조사 §9.3의 *"1000회 자동 테스트, 터널링 0건"* 의 자동화 버전입니다.

> 봇과 월드는 **같은 술어**를 씁니다. 초기 버전에서 봇의 "이 벽은 지나갔나?" 판정이 월드보다 0.012 느슨했고, 그것 때문에 반나절 동안 멀쩡한 차트를 의심했습니다. 그래서 봇을 공유 모듈로 뽑았습니다.

---

## 5. 성능

목표: **5년 된 폰에서 60fps 고정, 프레임당 4ms 이하.**

| 규칙 | 이유 |
|---|---|
| `shadowBlur` **전면 금지** | 구 빌드 렉의 주범. `npm test`가 회귀를 막습니다 |
| 렌더러 안에서 그라디언트 생성 금지 | 글로우와 비네트는 전부 스프라이트 캐시에 |
| 글로우 = 사전 렌더 비트맵 + `globalCompositeOperation='lighter'` | 블러 패스 0회 |
| 고정 240Hz 틱 / 렌더 분리 | 터널링 0, 프레임 무관 물리 |
| 벽 320개 오브젝트 풀 | 핫 루프 할당 0 |
| 적응형 품질 (DPR·깊이 레이어·파티클) | 프레임 피크 22ms 초과 시 자동 강등 |
| 오디오 클록 PLL | 시뮬레이션은 고정 틱, 장기적으로는 오디오에 락 |

측정: 데스크톱 1280×720 DPR2에서 프레임 피크 **1.0ms**.

오디오 클록이 rAF보다 오래 살아있으므로(백그라운드 탭), 두 클록이 0.6초 이상 벌어지면 **자동 일시정지**합니다. 못 본 벽으로 순간이동시켜 죽이지 않기 위해서입니다.

---

## 6. 리텐션 설계

| 장치 | 구현 |
|---|---|
| **즉시 재시도** | 사망 → 0.34초 → 결과. 결과 화면 아무 데나 탭 = 재시작 |
| **증분 성취** | %가 항상 보임. 진행바에 자기 최고 기록 눈금 |
| **연속 피드백** | 그레이즈 체인이 음정으로 올라감 — 화면을 안 봐도 들림 |
| **기록 갱신 즉시 알림** | NEW BEST / NEW HIGH SCORE / FIRST CLEAR |
| **연습 모드** | 자기 최고 기록까지 10% 단위로 시작 지점 선택 |
| **데일리 펄스** | 날짜 시드 → 전 세계 동일 차트. 공유 카드 |
| **해금** | 이전 트랙 60% 도달 |
| **반복 청취 피로 완화** | 곡이 절차 생성이라 섹션마다 편성이 바뀜 |

---

## 7. 법적 정리

자료조사 §12 준수:

- 이름 · 음악 · 음성 · 팔레트 **전부 자체 제작**
- 외부 에셋 0
- 구간 명칭도 자체 작명 (IGNITE / VECTOR / GRAZE / SEAL / DROP / PENTA …)
- **음원 라이선스 문제 완전 소멸** — 재생할 파일 자체가 없음

---

## 8. 남은 일

1. 실기기 3종 프레임 검증 (아이폰 저사양 포함)
2. Capacitor / PWA 래핑 → App Store 제출
3. 온라인 리더보드 (데일리 펄스 기반)
4. 트랙 4~6번 추가
5. 히든 트랙 조건 확정 (무사망 + 고HEAT 클리어)
